```
┌─────────────────────────────────────────────────────────┐
 __________   ____________   _____       ____   ____________     ________
|____   ___|  |____   ____|  |    \      |   |  |    ____   )   |   ___   )
    |   |         |   |      |     \     |   |  |   ()___()  )  |   |   )   )
    |   |         |   |      |   |\  \   |   |  |   | ______)   |   |    )   )
    |   |         |   |      |   | \   \ |   |  |   |           |   |     )  )
( (_/   /     ____|   |____  |   |  \   \|   |  |   |           |   |___ )  )
(__ _ _/     |____________|  |___|   \_______|  |___|           |__________)
    

└─────────────────────────────────────────────────────────┘
```

Welcome to JINPD!

## 🤖 With AI

Thank you for visiting JINPD. This is a multilingual chatbot built using Gemini in the Arkain environment. It automatically detects the user's language and responds in that language. This is the first chatbot built using Vibe coding.

**PROJECT STRUCTURE**
my_chatbot_project/
├── app.py                # Flask Main application (backend logic)
├── requirements.txt      # List of required libraries
├── static/
│   ├── css/
│   │   └── style.css     # Dialog design (UI/UX style)
│   ├── images/
│   │   ├── bot_icon.png  # (for icon)
│   │   └── bot_main.png  # (for main profile)
│   └── js/
│       └── chat.js       # Real-time conversation processing (frontend logic)
└── templates/
    └── index.html        # Main web page layout
├── .env                  # [Added] Save API keys and important settings


🛠️ **How to run in Arkain environment**
🚀Try typing them in order in the terminal:
   1. Create a virtual environment: 
       python3 -m venv venv
       
   2. Activate the virtual environment:
       source venv/bin/activate

   3. Reinstall the package:
       pip install -r requirements.txt


🛠️ **How to run in Terminal (important)**
🚀 Terminate existing processes: Press Ctrl + C in the terminal window to terminate the currently running server.
   1. Check your virtual environment: Check if (venv) is listed on the left side of the                terminal. If not, run the following command first.
      "source venv/bin/activate"
   2. Run server:
       "python app.py"

   If activation fails (recreation)
If nothing happens or an error occurs after entering the above command, the most reliable solution is to create a new virtual environment. Enter the commands below in order.    
    1. Delete the existing venv folder (optional): rm -rf venv
    2. Create a new virtual environment:
    "python3 -m venv venv"
    3. Reactivate the virtual environment:
    "source venv/bin/activate"
    4. Reinstall required libraries: If you have created a new virtual environment, you will            need to reinstall the required libraries.
    "pip install -r requirements.txt"


🛠️ **Last but not least, check the port**
🛠️ **If you have any questions, please E-mail me. (ai.jinpd@gmail.com)**



    





To watch usage guides, visit [https://docs.arkain.io](https://docs.arkain.io/).
